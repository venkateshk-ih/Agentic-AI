// Minimal entrypoint for the Agentic AI Tester project

async function main() {
  console.log('Agentic AI Tester skeleton starting...');
  console.log('Run `npm run generate` after implementing the generator.');
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
