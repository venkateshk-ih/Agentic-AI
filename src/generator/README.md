Generator module

This module will take requirement JSON produced by Agent A and retrieved context from the RAG service and produce Playwright TypeScript test code. The generator will produce block-anchored code sections so Agent C can request targeted patches on failures.